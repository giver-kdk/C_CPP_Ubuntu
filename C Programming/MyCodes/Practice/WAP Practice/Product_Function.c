#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
#include <string.h>
// #include <graphics.h>
int find_product(int a, int b);
int main()
{
	int x, y, multiple;
	printf("Enter any two numbers: ");
	scanf("%d %d", &x, &y);
	
}
int find_product(int a, int b)
{
	int product;
	product = a * b;
	return(product);
}