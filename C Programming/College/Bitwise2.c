#include <stdio.h>
#include <conio.h>
void main()
{
    int a, b, AND, OR, XOR;
    a= 65;
    b= 100;
    AND= a&b;
    OR=a|b;
    XOR=a^b;
    printf("The AND result is %d", AND);
    printf("\nThe OR result is %d", OR);
    printf("\nThe XOR result is %d", XOR);

}