#include <stdio.h>
#include <conio.h>

int main()
{
    int i, j, k, n;
    printf("Enter any number: ");
    scanf("%d", &n);
    for(i=1; i<=n; i++)
    {
        printf("%d", i);
        printf("%d", i);
        printf("%d", i);
    }
}