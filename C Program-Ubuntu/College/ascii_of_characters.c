#include <stdio.h>
void main()
{
    char ch='A';                            //character variable is giver charcter value inside ''
    printf("The ASCII value is %d", ch);    /*%d is for integer. So, it will represent the ASCII value of character 
                                             instead of the charcter itself. For charcter we use %c*/        
}